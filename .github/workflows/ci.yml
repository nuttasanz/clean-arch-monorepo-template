# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true

# env:
#   TURBO_TELEMETRY_DISABLED: 1

# jobs:
#   # ─── Lint & Type-check ──────────────────────────────────────────────────────
#   lint:
#     name: Lint & Type-check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9.0.0
#           run_install: false

#       - uses: actions/setup-node@v4
#         with:
#           node-version-file: .nvmrc
#           cache: pnpm

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - uses: actions/cache@v4
#         with:
#           path: .turbo
#           key: turbo-${{ runner.os }}-${{ github.sha }}
#           restore-keys: |
#             turbo-${{ runner.os }}-

#       - name: Lint & type-check
#         run: pnpm turbo run lint check-types

#   # ─── Security Audit ─────────────────────────────────────────────────────────
#   audit:
#     name: Security Audit
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9.0.0
#           run_install: false

#       - uses: actions/setup-node@v4
#         with:
#           node-version-file: .nvmrc
#           cache: pnpm

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Audit production dependencies
#         run: pnpm audit --audit-level=high --prod

#   # ─── Unit Tests ─────────────────────────────────────────────────────────────
#   test:
#     name: Unit Tests
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9.0.0
#           run_install: false

#       - uses: actions/setup-node@v4
#         with:
#           node-version-file: .nvmrc
#           cache: pnpm

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - uses: actions/cache@v4
#         with:
#           path: .turbo
#           key: turbo-${{ runner.os }}-${{ github.sha }}
#           restore-keys: |
#             turbo-${{ runner.os }}-

#       - name: Run tests
#         run: pnpm turbo run test

#   # ─── Migration Drift Check ──────────────────────────────────────────────────
#   migration-drift:
#     name: Migration Drift Check
#     runs-on: ubuntu-latest
#     needs: [lint, audit, test]
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9.0.0
#           run_install: false

#       - uses: actions/setup-node@v4
#         with:
#           node-version-file: .nvmrc
#           cache: pnpm

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Generate migrations from current schema
#         working-directory: apps/api
#         run: pnpm db:generate

#       - name: Fail if migrations are not committed
#         run: |
#           git diff --exit-code apps/api/src/infrastructure/database/migrations/ \
#             || (echo "::error::Uncommitted migration drift detected. Run 'pnpm db:generate' in apps/api and commit the result." && exit 1)

#   # ─── Docker Build — API ─────────────────────────────────────────────────────
#   docker-api:
#     name: Docker Build — API
#     runs-on: ubuntu-latest
#     needs: migration-drift
#     steps:
#       - uses: actions/checkout@v4

#       - uses: docker/setup-buildx-action@v3

#       - name: Build API image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: apps/api/Dockerfile
#           push: false
#           load: true
#           tags: repo/api:${{ github.sha }}
#           cache-from: type=gha,scope=docker-api
#           cache-to: type=gha,mode=max,scope=docker-api

#   # ─── Docker Build — Web ─────────────────────────────────────────────────────
#   docker-web:
#     name: Docker Build — Web
#     runs-on: ubuntu-latest
#     needs: migration-drift
#     steps:
#       - uses: actions/checkout@v4

#       - uses: docker/setup-buildx-action@v3

#       - name: Build Web image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: apps/web/Dockerfile
#           push: false
#           load: true
#           tags: repo/web:${{ github.sha }}
#           build-args: BACKEND_API_URL=http://localhost:3001
#           cache-from: type=gha,scope=docker-web
#           cache-to: type=gha,mode=max,scope=docker-web

#   # ─── Container Scan — API ───────────────────────────────────────────────────
#   scan-api:
#     name: Container Scan — API
#     runs-on: ubuntu-latest
#     needs: docker-api
#     permissions:
#       security-events: write
#     steps:
#       - uses: actions/checkout@v4

#       - uses: docker/setup-buildx-action@v3

#       # Images don't survive between jobs — rebuild from GHA BuildKit cache (near-instant)
#       - name: Rebuild API image for scan
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: apps/api/Dockerfile
#           push: false
#           load: true
#           tags: repo/api:${{ github.sha }}
#           cache-from: type=gha,scope=docker-api

#       - name: Scan API image with Trivy
#         uses: aquasecurity/trivy-action@0.24.0
#         with:
#           image-ref: repo/api:${{ github.sha }}
#           format: sarif
#           output: trivy-api.sarif
#           severity: HIGH,CRITICAL
#           exit-code: '1'
#           ignore-unfixed: true

#       - name: Upload Trivy results to GitHub Security
#         uses: github/codeql-action/upload-sarif@v3
#         if: always()
#         with:
#           sarif_file: trivy-api.sarif
#           category: trivy-api

#   # ─── Container Scan — Web ───────────────────────────────────────────────────
#   scan-web:
#     name: Container Scan — Web
#     runs-on: ubuntu-latest
#     needs: docker-web
#     permissions:
#       security-events: write
#     steps:
#       - uses: actions/checkout@v4

#       - uses: docker/setup-buildx-action@v3

#       - name: Rebuild Web image for scan
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: apps/web/Dockerfile
#           push: false
#           load: true
#           tags: repo/web:${{ github.sha }}
#           build-args: BACKEND_API_URL=http://localhost:3001
#           cache-from: type=gha,scope=docker-web

#       - name: Scan Web image with Trivy
#         uses: aquasecurity/trivy-action@0.24.0
#         with:
#           image-ref: repo/web:${{ github.sha }}
#           format: sarif
#           output: trivy-web.sarif
#           severity: HIGH,CRITICAL
#           exit-code: '1'
#           ignore-unfixed: true

#       - name: Upload Trivy results to GitHub Security
#         uses: github/codeql-action/upload-sarif@v3
#         if: always()
#         with:
#           sarif_file: trivy-web.sarif
#           category: trivy-web
