import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = {
  title: "Register | My Project",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
